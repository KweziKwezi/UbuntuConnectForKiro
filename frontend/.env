# See Properties/launchSettings.json in the backend project — this matches
# the "http" launch profile (applicationUrl). If you run the "https"
# profile instead, change this to https://localhost:7178/api.
VITE_API_URL=http://localhost:5275/api
